{
  "name": "deploy",
  "version": "1.0.0",
  "description": "Handles deployments locally and remote",
  "main": "index.js",
  "scripts": {
    "start:dev" : "npx micro-proxy -r rules.json -p 9000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "micro-proxy": "^1.1.0"
  }
}
