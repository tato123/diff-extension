import styled from 'styled-components';

const HR = styled.hr`
  outline: none;
  border: none;
  box-shadow: inset 0 0 1px var(--border-color);
  height: 1px;
  margin: 16px 0;
  display: block;
  position: relative;
`;

export default HR;
