import storage from './storage';
import url from './url';
import tabs from './tabs';
import runtime from './runtime';
import auth from './auth';

export default {
  storage,
  url,
  tabs,
  runtime,
  auth
};
