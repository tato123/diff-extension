import styled from "styled-components";
import { Header } from "@diff/shared-components";

const TightHeader = styled(Header)`
  font-weight: 500 !important;
  margin: 0;
`;

export default TightHeader;
