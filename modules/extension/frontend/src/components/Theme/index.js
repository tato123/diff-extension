const colors = {
  textColor: "#fff",
  border: "rgba(255,255,255,0.4)"
};

const size = {
  sm: "8px"
};

const text = {
  fontFamily: `"Inter UI", sans-serif`,
  lineHeight: 1.5,
  size: {
    small: "0.7em",
    normal: "1em"
  }
};

const Theme = {
  colors,
  text,
  size
};

export default Theme;
